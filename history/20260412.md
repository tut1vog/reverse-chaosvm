# 2026-04-12

## Phase 32 planning: Switch cache key to source hash

**Outcome**: plan created

**What was done**: User identified that TDC_NAME is unreliable as cache key — same TDC_NAME can map to different XTEA params (confirmed in 28.13/28.14). New approach: strip eks from tdc.js source, compute SHA-256 hash, use as cache key. This ensures builds with identical code share entries regardless of per-session eks variation.

**Notes**: 3 tasks planned: implement hash key (32.1), tests (32.2), clear cache + live test (32.3).

## 32.1: Switch cache key from TDC_NAME to source hash

**Outcome**: passed

**What was done**: Added `computeSourceHash(source)` to `scraper/tdc-utils.js` — strips eks assignment values via regex, computes SHA-256, returns first 16 hex chars. Updated `scraper/scraper.js` to use `sourceHash` for all cache lookup/store/autoPort calls while keeping TDC_NAME for logging. Updated `scraper/template-cache.js` `seed()` to read full target files, compute source hash, and use as cache key.

**Verification**: All 3 files pass syntax check. Code review confirms eks stripping, hash-based cache operations, and preserved TDC_NAME logging. 8 test failures expected — tests still use TDC_NAME-based lookups against seed().

**Notes**: Test fixes deferred to 32.2.

## 32.2: Tests for source hash cache key

**Outcome**: passed

**What was done**: Fixed 8 broken tests in `test-pipeline-integration.js` (2) and `test-scraper-foundation.js` (6) by replacing TDC_NAME-based lookups with `computeSourceHash(source)` lookups. Added 6 new tests for `computeSourceHash`: determinism, 16-hex-char format, eks stripping (literal and variable forms), code differentiation, and real target file stability.

**Verification**: `npm test` 261/263 (6 new tests added, 8 fixed, 2 known failures). All 18 auto-port tests still pass.

**Notes**: Test count went from 257 to 263 (6 new computeSourceHash tests).

## 32.3: Clear cache and live test

**Outcome**: passed

**What was done**: Cleared `scraper/cache/templates.json` to `{}`, then ran `seed()` which re-populated from existing `output/*/pipeline-config.json` files using source hash keys. 6 entries seeded with 16-char hex hash keys.

**Verification**: `npm test` 261/263 (baseline maintained). Cache contains 6 entries keyed by source hash. One entry (`3e77d1890dff73ab`, 98 opcodes) has stale keyMods `[0,0,0,0]` — will be corrected when auto-port re-extracts from live source.

**Notes**: Phase 32 complete. Cache now uses SHA-256 source hash keys. Live scraper will auto-port any template not in cache, producing correct params via pipeline.

## 32.3 (continued): Remove structural lookup fallback

**Outcome**: passed

**What was done**: 
1. Re-ran pipeline on `targets/tdc-live.js` — old extraction had `keyMods: [0,0,0,0]`. Fresh extraction: `keyMods: [1579040,0,2829060,0]`, hashPosition 47, headerSplit byte-boundary.
2. Removed `lookupByStructure()` fallback from `solveCaptcha()`. Each build has unique XTEA keys — structural matching by opcode count returns wrong keys, causing errorCode 12. Cache miss now goes directly to auto-port.
3. Removed `parseVmFunction` import (no longer used in scraper).
4. Updated integration tests: removed structural match test, updated resolution chain to `lookup → autoPort`.

**Verification**: `npm test` 261/263 (17 auto-port tests pass, 2 known failures). Syntax OK.

**Notes**: The structural lookup was the primary cause of errorCode 12 — it silently served wrong XTEA params from reference builds for live builds with the same opcode count but different keys.

## Phase 33 planning: TDC Survey — Collect Live Template Data

**Outcome**: plan created

**What was done**: User wants to clear cache and run scraper repeatedly to collect data about Tencent's live tdc.js rotation pool. Script will record which builds port successfully, which fail (and why), and which still return errorCode 12. Data saved to output/tdc-survey/ for analysis.

**Notes**: 3 tasks: create survey script (33.1), tests (33.2), run survey + analyze (33.3).

## 33.1: Create tdc.js survey script

**Outcome**: passed

**What was done**: Created `scripts/tdc-survey.js` (539 lines) — standalone survey tool that performs N independent CAPTCHA attempts with fresh sessions, recording tdc.js build metadata (TDC_NAME, sourceHash, source length, opcode count), auto-porting new builds via pipeline, and running full CAPTCHA solve flow for ported builds. CLI args: `--attempts N`, `--verbose`, `--save-sources`, `--delay MS`. Outputs summary table to stdout and `output/tdc-survey/report.json`.

**Verification**: Syntax OK. npm test 261/263 (baseline). Code review: all failure modes handled (vm parse, pipeline, solve, verify, session errors). Sources saved on `--save-sources`. Report.json produced with correct structure. Subagent live-tested: successful CAPTCHA solve (errorCode=-1).

**Notes**: Script replicates scraper's full flow (POST fields, behavioral events, slideSd, collect, vData) but wires it manually for survey instrumentation rather than using Scraper class.

## 33.2: Tests for survey script

**Outcome**: passed

**What was done**: Added `tests/test-tdc-survey.js` with 7 tests across 2 describe blocks: parseArgs (defaults, all flags, partial flags) and buildPostFields (38-key count, required fields, base64 UA, value pass-through). Added conditional exports guard in `scripts/tdc-survey.js` for testability.

**Verification**: `node --test tests/test-tdc-survey.js` — 7/7 pass. `npm test` — 261/263 (baseline, 2 known failures).

**Notes**: None.

## 33.3: Run survey and analyze results

**Outcome**: passed

**What was done**: Ran `node scripts/tdc-survey.js --attempts 30 --verbose --save-sources`. Survey completed successfully, discovering 10 unique tdc.js builds across 30 attempts.

**Verification**: Survey completed without fatal errors. Report saved to `output/tdc-survey/report.json`. All 10 unique sources saved to `output/tdc-survey/`.

**Key findings**:
- 10 unique builds in Tencent's rotation pool (30 attempts)
- 7/10 port successfully via pipeline; 3/10 fail vm-parse ("Could not identify thisCtx variable") — these are large builds (162K-202K chars), likely a new VM architecture
- Template distribution: 1× A (95 ops), 3× B (94 ops), 3× unknown (96/98 ops), 3× unparseable
- errorCode -1 (success) achieved on 3 distinct builds (e6a45ba6, 3e77d189, 83d7be69), proving the token generator works for multiple live builds
- errorCode 12 (token rejected) is the dominant result even for ported builds — inconsistent (same build sometimes -1, sometimes 12), suggesting timing/freshness or field-order issues
- errorCode 9 seen once (wrong slider answer — OpenCV miss)
- Each build has a unique XTEA key (confirmed: no two builds share the same key)

**Notes**: Phase 33 complete. Two clear next priorities: (1) fix vm-parser for large 162K-202K builds, (2) investigate errorCode 12 inconsistency.

## 34.1: Fix extractThisCtx and extractCatchVars for obfuscated builds

**Outcome**: passed

**What was done**: Two fixes in `pipeline/vm-parser.js`:
1. `extractThisCtx`: added bracket-notation match — `property.type === 'Literal' && property.value === 'call'` alongside existing `Identifier` check.
2. `extractCatchVars`: added `pcVar` parameter and fallback heuristic — when `.pop()` pattern fails (obfuscated method names), look for `pcVar = someArray[anything]()` assignment in catch body.

**Verification**: All 3 previously-failing builds now parse: 27dda893 (103 cases), 3429444f (91 cases), e2170903 (93 cases) — all 6 variables extracted. Existing builds (tdc.js, tdc-v2.js, tdc-v5.js) still parse correctly. npm test 261/263 baseline.

**Notes**: Root cause was NOT a new VM architecture — same dispatch structure, just obfuscated property access (bracket notation + runtime string decoder). The fix is fully backward-compatible.

## 34.2: Tests for vm-parser obfuscation fixes

**Outcome**: passed

**What was done**: Added 4 tests to `tests/test-vm-parser.js` in a new `describe('vm-parser: obfuscated builds')` block. Tests parse the 3 survey builds (103, 91, 93 cases) and verify all 6 variables are extracted with `_0x`-prefixed names.

**Verification**: 35/35 vm-parser tests pass. npm test 265/267 (4 new tests added, 2 known failures).

**Notes**: Tests use `it.skip` fallback if survey files are missing.
