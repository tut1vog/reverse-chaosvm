# 2026-04-11

## 17.1: Chrome cd injection script

**Outcome**: passed

**What was done**: Created `scripts/chrome-cd-inject.js` (829 lines) — Puppeteer script that intercepts Chrome's raw cd array via JSON.stringify hook before encryption, feeds it into standalone `generateCollect()` via new `cdArrayOverride` option, and submits via Chrome TLS. Modified `scraper/collect-generator.js` to accept `cdArrayOverride` in options, skipping `buildDefaultCdArray` and `reorderCdArray` when provided. Also re-exported `buildDefaultCdArray` for comparison use.

**Verification**: `node -c` syntax check passed; `npm test` 163/165 (baseline); cdArrayOverride logic confirmed at lines 347-348; JSON.stringify interceptor at lines 183-194; cd comparison and override injection at lines 462-495.

**Notes**: Script uses `page.evaluateOnNewDocument` to install the interceptor before tdc.js loads, capturing any array with length 55-65 (the cd fingerprint array). Falls back to standalone cd generation if Chrome capture fails.

## 17.2: Tests for Chrome cd injection script

**Outcome**: passed

**What was done**: Added 10 tests in 4 describe blocks to `tests/test-scraper-foundation.js` covering cdArrayOverride: produces different output (2), uses provided array with determinism (2), non-array values ignored/fallback (4), skips reorderCdArray when set (2).

**Verification**: `npm test` 173/175 (10 new tests added to 163/165 baseline, same 2 known failures).

**Notes**: Tests placed in test-scraper-foundation.js alongside existing collect-generator tests rather than a new file.

## 17.3: Full Chrome cd injection — live test (attempt 1)

**Outcome**: failed

**What was done**: Ran `scripts/chrome-cd-inject.js` live. 3 attempts executed. cd array capture via JSON.stringify hook failed on all attempts — the hook never fired.

**Verification**: 
- Attempt 1: TDC_NAME `DkPDkCnAekYMgVghTDOeSKmVZbkVCQUG` — unknown template, VM parse failed
- Attempt 2: TDC_NAME `hDWlWNMRnBmeDRVkbhNbSOFBjAXJPHig` — Template A, reached verify, errorCode 9 (but used standalone cd fallback, not Chrome cd)
- Attempt 3: Same unknown template as attempt 1

**Notes**: Root cause identified: `docs/TOKEN_FORMAT.md` line 91 states cd uses "hand-rolled JSON concatenation (func_276), NOT JSON.stringify()" — this is an anti-hooking measure. The plan's JSON.stringify approach was fundamentally wrong. Remediation: decrypt Chrome's collect token using known XTEA params instead (Approach B). Also discovered new unported template `DkPDkCnAekYMgVghTDOeSKmVZbkVCQUG` in rotation.

## 17.3.1: Fix cd capture — decrypt Chrome's collect token

**Outcome**: passed

**What was done**: Replaced the broken JSON.stringify interceptor in `scripts/chrome-cd-inject.js` with XTEA decryption approach. Added `decryptXtea()` and `decryptCollect()` functions (from collect-diff.js). Chrome's collect token (from `TDC.getData(true)`) is now decrypted after template cache lookup to extract the raw cd array. Removed all `evaluateOnNewDocument` hooks and `__capturedCdArray` references.

**Verification**: `node -c` passes; `npm test` 173/175; zero matches for old hook references; decryption flow confirmed at lines 397→457-464→547.

**Notes**: Decryption happens at Step 7b, after template cache provides XTEA params. Falls back to standalone cd if decryption fails (e.g., unknown template).

## 17.3: Full Chrome cd injection — live test (attempt 2, with decryption fix)

**Outcome**: passed (experiment conclusive, errorCode 9 persists)

**What was done**: Ran `scripts/chrome-cd-inject.js` with XTEA decryption approach. 3 attempts: attempt 1 successfully decrypted Chrome's cd (60 fields from 98-opcode template), injected via cdArrayOverride, submitted → errorCode 9. Attempts 2-3 decryption failed (garbled output).

**Verification**: Script ran to completion on all 3 attempts. Attempt 1: cd decrypted successfully (60 fields), 57/60 differ from standalone, errorCode 9. Results saved to output/chrome-cd-inject.json.

**Notes**: 
- **Critical finding: cd values alone are NOT the root cause of errorCode 9.** Chrome's exact cd values + standalone XTEA encryption still rejected.
- 57/60 cd field differences indicate 98-opcode templates use completely different collector schema ordering
- The issue likely lies in XTEA encryption params (keyMods), token assembly/segment layout, or sd structure differences
- Tasks 17.4-17.6 (binary search cd fields, fix fields, re-test) superseded — wrong direction
- Live server rotation: 3 different TDC_NAMEs across 3 attempts, including two unknown 98-opcode templates
