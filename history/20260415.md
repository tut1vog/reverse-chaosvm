# 2026-04-15

## 44.4: Per-field value-source pin + pre-cipher transform check

**Outcome**: passed

**What was done**: Subagent ran the split-on-`&/=` pre-check (outcome 2 — garbled keys), then decompiled webpack module 40's `encryptData` (fn 13860) into a 3-stage chain: PKCS#7-style padder (fn 13989, alphabet `"0abcdefghijklmnop"`, `padLen = 16 - (len % 16)`), AES-ShiftRows-style 16-byte permutation (fn 14153, `PERM = [0,4,8,12,5,9,13,1,10,14,2,6,15,3,7,11]`), and Phase 43's XTEA (module 41). Inverted the permutation to recover clean `key=value` pairs from both fixtures. Decompiled 3 of fn 22317's 4 nested helpers: fn 22400 builds `tp` (tp-probe), fn 22730 builds the digit-array consumed by `key` (via `require(18)(body,"tlg")` → char-lookup loop), fn 23399 builds `ss`. Other 5 fields inline in fn 22317: `version` = `"2"` literal, `py` = `arguments[1].py`, `env` = `require(0)() ? "0" : "1"`, `inf` = `window===window.top ? "top":"iframe"`, `cLod` = TDC state probe (`loadTDC` / `unloadTDC`). Delivered `research/vm-slide-stack-vm/FINGERPRINT-SCHEMA.md` (6 sections) and `research/vm-slide-stack-vm/build-fingerprint-plaintext.js` (reference impl, not productized).

**Verification**: Independently re-ran `encodeVData(buildFingerprintPlaintext({obj, order}))` against both committed fixtures. All four assertions passed byte-identically: jsdom plaintext match, jsdom vdata match, HAR plaintext match, HAR vdata match. FINGERPRINT-SCHEMA.md present with all six required sections. build-fingerprint-plaintext.js exports `buildFingerprintPlaintext` as required. No modifications to protected paths (`targets/`, `sample/`, `tools/vdata-generator/`, `tests/fixtures/`, `docs/`, existing FN-*-DECOMPILE.md). npm test: 193/221 — unchanged from baseline (confirmed via stashed-check). The plan's `411/411` target was stale; actual baseline is 193/221 pre-existing, no regressions introduced.

**Notes**:
- Two new facts surfaced that may warrant plan restructuring before 44.5b:
  1. **Sort-order contradiction**: 44.2.6 reported fn 22317's `.sort()` at pc 23949 produces alphabetical order `[cLod,env,inf,key,py,ss,tp,version]`, but both committed fixtures ship non-alphabetical orders (jsdom `[inf,env,tp,key,py,ss,cLod,version]`, HAR `[inf,env,tp,cLod,version,key,ss,py]`). Either 44.2.6's reading of the sort call was wrong, or the comparator (fn 23898) is non-lexicographic — needs a re-look. Reference impl currently takes `order` as a caller input rather than computing it.
  2. **XTEA key drift**: `sample/vm_slide.js` bakes `34e2c8f07b5169ad` at pc 13931, but both committed fixtures encrypt with `2e430f8c15b7da96` (the Phase 43 constant). The committed fixtures are from a different vm-slide build than `sample/vm_slide.js`.
- Director stopping for user review before dispatching next task — these two findings may affect 44.4.5 / 44.5b scope.
- `cLod` state-machine region (pcs 23059..23224) is not exhaustively walked; fallback values `loadTDC`/`unloadTDC` observed in fixtures but full branching not documented.
- `require(18)` (module 18's role for the `key` field) not fully decompiled.

## 44.0.1: Bytecode build reconciliation — XTEA key drift

**Outcome**: passed

**What was done**: Subagent grepped `output/vm-slide/bytecode.json` (via per-char `OP_10`-run scan, not raw ASCII) for both key strings. Result: `34e2c8f07b5169ad` found at pcs 13931 and 15149 as 16 `OP_10` pushes inside `OP_04 ... OP_02 2 OP_49` array builders, then passed as an argument via `OP_58 13860 ...` at pc 13970; `2e430f8c15b7da96` NOT present anywhere in the bytecode (nor in `sample/vm_slide.js` source text). Subagent cross-checked `research/vm-slide-stack-vm/vdata-dynamic-trace.js` and found Phase 43 captured the key by sampling **local slot 4** of the encrypt closure's stack frame at the first dispatch tick of fn 13860 — i.e. after the function prologue's pre-cipher transform had run. Confirmed `tools/scraper/vdata-harness.js` and `research/vm-slide-stack-vm/vdata-dynamic-trace.js:48` both load `sample/vm_slide.js` by default, and the HAR fixture's `xtea_key_hex` matches the jsdom fixture's — so `sample/vm_slide.js` is unambiguously the fixture-generating build. Delivered `research/vm-slide-stack-vm/BUILD-RECONCILE.md` (~2 pages).

**Verification**: Director spot-checked disassembly pcs 13921..14008 via sed — confirmed `OP_04` at 13930, 16 `OP_10 <charcode>` pushes at 13931..13961 exactly matching the ASCII bytes of `34e2c8f07b5169ad`, `OP_02 2 OP_49` at 13963..13965, `OP_58 13860 3 1 7 10 8 11 10 9 3` at 13970. Matches the subagent's report verbatim. BUILD-RECONCILE.md present with verdict (A)+nuance clearly stated, evidence organized into 4 labeled sections, implications/nomenclature/cross-checks documented. No modifications to protected paths.

**Notes**:
- Verdict: **(A) with nuance** — `sample/vm_slide.js` IS the fixture-generating build; BOTH keys are real and from the same build. `34e2c8f07b5169ad` is the **bytecode-literal pre-cipher seed** (pushed at pcs 13931 and 15149 into fn 13860); `2e430f8c15b7da96` is the **runtime XTEA round key** observed in the encrypt closure's local 4 after fn 13860's prologue has transformed the seed. 44.4's "Finding 2" is reclassified, not dismissed.
- No re-decode of the fixture build needed; all Phase 44 pc anchors (fn 22317, 13860, 13989, 14153, 22400, 22730, 23399) stand valid.
- Recommended nomenclature for Phase 44 downstream work: `xtea_seed_hex` for the bytecode literal vs `xtea_key_hex` for the runtime round key.
- Pinning the seed→key transform itself is a new Phase 44 follow-up — not required for 44.4.1 or 44.5b, since the reference encoder already uses the runtime key.
- CLAUDE.md Project Memory §Phase 42 corrected: previous wording claimed `2e430f8c15b7da96` was the "bytecode constant" — that was imprecise. Now correctly distinguishes runtime key vs seed with a pointer to BUILD-RECONCILE.md.

## 44.4.1: Sort-order contradiction resolution

**Outcome**: passed

**What was done**: Subagent re-read fn 22317 around pc 23949 and confirmed the sort call site exactly where 44.2.6 reported it: pcs 23885..23894 stage the `[arr,"sort"]` pair, pc 23895 `OP_06 23945` jumps over the closure body, pc 23945 `OP_58 23898 0 0` creates the comparator closure, pc 23949 `OP_02 1` is the `arr.sort(cmp)` METHOD_CALL. Then walked fn 23898's body `[23898..23944]` end-to-end: pcs 23902..23925 build `Math.random` via `OP_04`+`OP_10` string pushes → `OP_32` MAKE_GLOBAL_REF → `OP_41` GET_PAIR, pc 23926 `OP_02 0` invokes `Math.random()`, pc 23928 `OP_08 0.5` + pc 23930 `OP_31` GT, pc 23931 `OP_60 23941` JUMP_IF_TRUE branches, the false arm (pcs 23933..23938) pushes `0` via `OP_50 0` then subtracts `1` → `-1`, the true arm (pcs 23941..23942) pushes `1`, pc 23944 `OP_16` RETURN. Verdict: **hypothesis (ii)** — the comparator is the textbook JS Fisher-Yates anti-pattern `Math.random() > 0.5 ? 1 : -1`, so vm-slide shuffles the 8-key schema randomly per call. The committed fixtures' orders `[inf,env,tp,key,py,ss,cLod,version]` (jsdom) and `[inf,env,tp,cLod,version,key,ss,py]` (HAR) are just two draws from the shuffle. Updated `research/vm-slide-stack-vm/build-fingerprint-plaintext.js` so `order` is optional: when omitted, defaults to `Object.keys(obj)` (JS preserves string-key insertion order). Delivered `research/vm-slide-stack-vm/SORT-ORDER-RESOLUTION.md` with full pc citations.

**Verification**: Director spot-checked `disassembly-full.txt` pcs 23898..23951 — exact byte-for-byte match with the subagent's walk (confirmed `OP_40 3`, `OP_42 2`, `OP_10 77/97/116/104` for "Math", `OP_10 114/97/110/100/111/109` for "random", `OP_02 0`, `OP_08 0.5`, `OP_31`, `OP_60 23941`, `OP_50 0`, `OP_08 1`, `OP_21`, `OP_06 23944`, `OP_05`, `OP_08 1`, `OP_16`, `OP_58 23898 0 0`, `OP_02 1`). Ran the updated builder against both committed fixtures with no `order` argument: inverse-permuted each fixture's stored ciphertext via `PERM`, parsed the unpadded `key=value&...` string into an insertion-ordered `obj`, then called `buildFingerprintPlaintext({obj})`. Both fixtures round-trip byte-identical to the stored `plaintext_hex` / `har_decrypted_plaintext_hex`. Observed keys match the file's `FIELD_ORDER_JSDOM`/`FIELD_ORDER_HAR` constants exactly. No modifications to protected paths (`targets/`, `sample/`, `tools/vdata-generator/`, `tests/fixtures/`, `docs/`).

**Notes**:
- The sort call site (pc 23949) and comparator ID (fn 23898) that 44.2.6 reported were both correct. What 44.2.6 missed was the comparator's nature — not lexicographic, randomised.
- 44.5b's "synthesize from `obj` alone" cannot have a canonical default order. The only deterministic paths are (a) accept a pre-ordered obj and trust insertion order (fixture-replay path), or (b) seed an RNG and call it like the real vm-slide does (from-scratch live path). This constraint should be reflected in 44.5b's task spec when it's drafted.
- Tests that reproduce a captured fixture must build the obj in observed order; they must not depend on `Math.random()`.
- Existing `{obj, order}` signature still works as an explicit override — no breakage to the Phase 44.4 test path.

## 44.4.5: Orchestrator `getCaptchaData` invocation site — premise disproven

**Outcome**: passed (research goal achieved in the negative — plan-impacting finding)

**What was done**: Subagent grepped `sample/t_captcha_slide.js` (213,162 bytes, webpack 4 bundle) for `getCaptchaData`, `CaptchaData`, `captchaData`, `getCaptcha`, `chaos`, `TENCENT_CHAOS`, `getVData`, `vData`. Result: **zero** occurrences of anything `getCaptchaData`-related. The only vData-related hits are `getVData` ×2 and `vData` ×1, all clustered at bytes 163046..163091 inside module 56. The enclosing source is an IIFE at bytes 162929..163108 gated entirely on `a.isLowIE()`: `if (a.isLowIE()) { ...serialise d into n=k+"="+v[]; var o = window.getVData && window.getVData(n.join("&")); o && (e.vData=o) }`. On Chrome this block is a no-op; vData must come from the XHR monkey-patch (Phase 42's original finding). Cross-checked against `output/vm-slide/vdata-callgraph.json`: every one of the 14 runtime encrypt calls for the committed HAR fixture passes through fn 15918 (containing range `[15918, 16230]`); fn 22317 does NOT appear in the runtime trace at all. Delivered `research/captcha-orchestrator/GETCAPTCHADATA-CALLSITE.md` (~280 lines).

**Verification**: Director ran `node -e` to count literal substrings in `sample/t_captcha_slide.js` — confirmed `getCaptchaData: 0`, `CaptchaData: 0`, `chaos: 0`, `TENCENT_CHAOS: 0`, `getVData: 2` (last index 163063), `vData: 1` (last index 163091). Read bytes 162850..163200 and confirmed the `!function(e){if(a.isLowIE()){...window.getVData(n.join("&"))...}}(d)` IIFE exactly as described. Read `output/vm-slide/vdata-callgraph.json` — confirmed 14 runtime-caller PCs all at pc 16182, `containing_entry: 15918`, `containing_range: [15918, 16230]`, so fn 22317 is not in the live callgraph. Research note exists with call-site search table, verbatim source snippet, reversed oracle cross-check, and dedicated contradictions section. No protected path mods.

**Notes — plan-impacting implications**:
- **Phase 42's mechanism stands.** The XHR monkey-patch on Chrome / `getVData` on IE9 narrative in `docs/CAPTCHA_ORCHESTRATOR.md` §6.2/§517 is supported by the orchestrator source, not contradicted by it. 44.2.6's reclassification of that mechanism as "likely obsolete" was wrong and needs to be reverted by Phase 44.7.
- **44.2.6's "fn 22317 = getCaptchaData (live)" conclusion is wrong on the Chrome path.** fn 22317 is stored to `n[4].getCaptchaData` as an internal vm-slide export, but nothing in the observed Chrome runtime reads it. fn 22317 is either dead code in the current build, reserved for an unobserved code path (different UA branch? different entry point?), or lives behind an indirect lookup not captured by the current runtime trace.
- **fn 20539 (proxyXHR send-handler, installed at pc 20808 as `XMLHttpRequest.prototype.send`) is the live Chrome producer**, not dead code as 44.2.6 claimed. It is the function that contains the fn 15918 invocation path the runtime trace actually records.
- **`build-fingerprint-plaintext.js` is still byte-identical to both fixtures**, which means the padder + ShiftRows permuter + XTEA + base64 pipeline is structurally correct — 44.2.6 correctly identified the pipeline shape but attached it to the wrong function. The real plaintext builder must be in fn 20539's callgraph (not fn 22317's), and it happens to construct a plaintext with the same shape.
- **Sort-order finding from 44.4.1 is also suspect.** If fn 22317 is not live, then fn 23898's `Math.random()` comparator is also not exercised at runtime. The per-run order randomness observed between jsdom and HAR fixtures must come from a different source in the real producer's callgraph (possibly object enumeration order differing between V8 builds, possibly a different shuffle in fn 20539's path, possibly just hashmap insertion variance). 44.4.1's reference-impl change (making `order` optional) is still correct for the replay path, but the "why is the order random" answer is no longer settled.
- **Plan revision required before dispatching further Phase 44 tasks.** Draft proposal to be surfaced to user: (1) reopen Phase 42's mechanism as confirmed, (2) reclassify fn 22317 as "internal export, unobserved on Chrome" pending a wider dynamic trace, (3) insert a new task to decompile fn 20539's body end-to-end and pin its plaintext-build pipeline, (4) reopen the per-run order question, (5) re-evaluate the remaining Phase 44 task list (44.3.5 / 44.5a / 44.5b / 44.6 / 44.7) against the new mental model.

---

## 44.2.7: fn 20539 full end-to-end decompile + real plaintext-build pin

**Outcome**: passed

**What was done**: Subagent extended `research/vm-slide-stack-vm/vdata-dynamic-trace.js` into a new standalone tracer `research/vm-slide-stack-vm/trace-fn20539-entry.js` that patches vm-slide's closure-entry tap and records first-call args for every closure entry in one live run; saved output to `output/vm-slide/fn-20539-entry-trace.json`. Walked fn 20539 and its install context statically. Confirmed: FUNC_CREATE `OP_58 20539 3 1 7 6 8 3` at pc 20797; install `OP_24` at pc 20808 writing the closure onto `XHR.prototype.send`; sibling `.open` wrapper fn 20353 installed at pc 20473 is the shared-guard writer (records `this` when `.open("/cap_union_new_verify")` is called, into fn 20140's slot-6 cell which fn 20539 reads as its inner slot 7). Runtime: fn 20539 receives the full 9345-byte form-encoded POST body as `arguments[0]`, calls slot 8 → fn 13860 with `(body, {py})`, receives rewritten body, passes to `savedSend.call`. Injection is body-append — final length 9504 = 9345 + `&vData=` (7) + 152-char base64 — done via STORE_LOCAL_REF at pc 20751. Discovered that `ancestor_chains` in `vdata-callgraph.json` are a tracer artifact (host-JS transitions from jQuery.ajax → native xhr.open → fn 20353 tail → native xhr.send → fn 20539 collapsed into the nearest VM tick), NOT a real call stack — so 44.4.5's "fn 20539 is the frame above fn 15918" reading was directionally right but mechanically wrong. **8-field schema pinned LIVE** from a runtime capture of fn 13860's 110-byte first arg: `env=1&key=qLCZ&version=2&cLod=unloadTDC&ss=0%2C&tp=Cannot read properties of null (reading 'src')&py=0&inf=top`. The field set `{env,key,version,cLod,ss,tp,py,inf}` matches 44.2.6's names — but it is a **tdc runtime-state probe**, not a browser fingerprint (`tp` is a captured JS runtime error, `inf` is iframe-position, `cLod` is a lifecycle marker). fn 15918 was observed entered with `(string:112, string:16)` and the 16-byte key is the literal `"34e2c8f07b5169ad"` (the bytecode literal seed, not the `2e430f8c15b7da96` runtime-key claim from Phase 42) — reconciliation owed. Confirmed fn 23898 (44.4.1's Math.random comparator) enters 15× with `(string:3, string:2)` and is a DOM helper unrelated to plaintext-order. Left unresolved: the exact FUNC_CREATE stored in fn 20140's slot 3 / fn 20539's inner slot 8 that calls fn 13860 directly — punted to a proposed new 44.2.8.

**Verification**: Deliverable `research/vm-slide-stack-vm/FN-20539-DECOMPILE-44.2.7.md` present with all six required sections (body range + install, pseudocode, caller chain, real fingerprint schema, injection site, Phase 44 mental-model rescoping), plus bonus §6b (pre-pad plaintext schema) and §10 (summary). Bytecode pcs 20473/20749/20751/20797/20808 independently re-verified against `output/vm-slide/bytecode.json` (correct opcodes: 24/66/36/58/24). FUNC_CREATE operand at pc 20798 = 20539 confirmed. Trace artifact `output/vm-slide/fn-20539-entry-trace.json` present with captured `fn_20539_first_args` (length 9345), `fn_15918_first_args` (length-112 permuted string + 16-byte key `34e2c8f07b5169ad`), and `all_entry_counts`. No modifications to protected paths (`targets/`, `sample/`, `tools/`, `tests/fixtures/`, `docs/`, `build-fingerprint-plaintext.js`, committed `vdata-dynamic-trace.js`). Only new files created.

**Notes**: The task originally planned to modify `vdata-dynamic-trace.js`; the agent chose instead to add a standalone tracer `trace-fn20539-entry.js`, which is strictly safer (no risk of breaking the existing oracle). Accepted. Three downstream Phase 44 tasks need rescoping based on the findings: 44.4.1 retires as unreached, 44.3.5 narrows to capture-only, and a new 44.2.8 is proposed to pin the last unresolved hop (fn 20140 slot-3 / fn 20539 inner-slot-8 → fn 13860). Presented as a rescoping proposal to the user; dispatch paused pending approval.

---

## 44.2.8: fn 20539 inner slot 8 hop identity + per-run order source

**Outcome**: passed — reversal-of-reversal.

**What was done**: Subagent walked fn 20140 (proxyXHR installer) to pin how its slot 3 is populated and discovered that slot 3 is bound from fn 20140's *single function argument* at entry (`OP_42 3`), not from an internal `OP_36 STORE_LOCAL_REF`. Tracked the caller chain: fn 20140 is called from `require(44).proxyXHR(arg)` at pc 19661 inside fn 19604 (a vm-slide-internal orchestrator / init function). That caller passes its own arg down — the init function is itself called with `getCaptchaData` as a parameter. Resolved the end-to-end chain as: `vm-slide-internal orchestrator → init(getCaptchaData) → require(44).proxyXHR(getCaptchaData) → fn 20140 binds it as slot 3 → fn 20539 captures slot 3 as inner slot 8 → fn 20539 pc 20749 OP_66 2 calls slot8(body, {py}) = fn 22317(body, {py})`. fn 22317 body `[22317..24233]` is exported by webpack module fn 20970 at pc 24252 as `module.exports.getCaptchaData` (FUNC_CREATE at pc 24234, argc=2). Walked fn 22317 end-to-end confirming the 8-field schema `["tp","key","py","env","version","cLod","inf","ss"]` built at pcs 22317..23394 (each field built by a different helper: 22400=tp, 22730=key-prep, 23399=ss, plus inline py/env/version/cLod/inf), the schema array constructed at pcs 23753..23894, fn 23898's `Math.random() > 0.5 ? -1 : 1` comparator created at pc 23945, `arr.sort(fn 23898)` at pc 23949, per-element `name=value` emit loop at pcs 23985..24084, `arr.join("&")` at pc 24161, fn 13860 `encryptData` call, and body-append emission of the final 9504-byte rewritten body. Delivered `research/vm-slide-stack-vm/FN-20539-SLOT8-HOP.md` (9 sections, 26 KB) with TL;DR, full walk, reconciliation to prior 44.2.x notes, and 44.5b unblock-plan.

**Verification**: Independently confirmed every pc claim against `output/vm-slide/bytecode.json`: pc 24234 = `OP_58 22317 6 2 17 10 18 12 19 9 20 11 21 8 22 7 3 4` ✓; pc 20749 = `OP_66 2` (2-arg call) inside fn 20539 ✓; pc 23945 = `OP_58 23898 0 0 2 1` ✓. Confirmed runtime entry counts in `output/vm-slide/fn-20539-entry-trace.json`: `all_entry_counts["22317"] = 1` (matches fn 20539 and fn 13860 — one call per instrumented send), `all_entry_counts["23898"] = 15` (TimSort comparison count for sorting an 8-element array — consistent with Node's TimSort implementation), `all_entry_counts["20140"] = 1`, `all_entry_counts["20970"] = 1`, `all_entry_counts["19604"] = 1`. All required sections present (slot-8 identity, wrapper walk, per-run order source, runtime cross-check, 44.5b unblock plan). No modifications to protected paths.

**Notes**: This is a reversal-of-reversal — 44.2.6 and 44.4.1 were structurally correct all along, and the two intervening remediations (44.4.5 + 44.2.7) were both based on incomplete readings of the available evidence. Specifically:

1. **44.4.5** searched `sample/t_captcha_slide.js` for the literal string `getCaptchaData`/`CaptchaData`/`chaos` and found zero, then concluded fn 22317 is not live. The error: `sample/t_captcha_slide.js` is the show-page's client-side orchestrator bundle, but the orchestrator that actually passes `getCaptchaData` into `proxyXHR` is INSIDE vm-slide itself (fn 19604). 44.4.5 grepped the wrong file. The string `getCaptchaData` DOES appear in vm-slide's bytecode as a string literal pushed into the module-export write at pc 24252 — searching `output/vm-slide/bytecode.json` would have caught this.

2. **44.2.7** checked `vdata-callgraph.json`'s ancestor chains (correctly identifying them as a tracer artifact) but did NOT consult its own `fn-20539-entry-trace.json` `all_entry_counts` field for fn 22317. fn 22317 was in the entry counts with value 1 all along — a one-line `all_entry_counts["22317"]` lookup would have immediately disproven 44.4.5's conclusion and avoided the entire 44.2.7→44.2.8 remediation chain. The entry-count check is now the recommended first-order liveness test for all future Phase 44+ research.

3. The Phase 44 discovery stream is now CLOSED. The full pipeline is pinned end-to-end: vm-slide internal orchestrator → init → require(44).proxyXHR → fn 20140 binds getCaptchaData as slot 3 → XHR patches install → on verify send, fn 20539 calls fn 22317(body, {py}), which builds the 8-field Math.random-shuffled kv string, encrypts via fn 13860, appends `&vData=` to the body, and returns the 9504-byte rewritten body to savedSend. Stream B (44.5a → 44.5b → 44.6 → 44.7) is unblocked. Plan revision pending user review to retire 44.4.1's earlier retirement, optionally defer 44.3.5, and re-expand 44.7's docs scope back toward the 44.2.6 framing that turned out to be right.

---

## 44.5a: Replay-with-substitution vData builder

**Outcome**: passed

**What was done**: Subagent shipped `tools/vdata-generator/build-plaintext.js` (port of `research/vm-slide-stack-vm/build-fingerprint-plaintext.js`'s kv-build + PKCS#7 pad + ShiftRows permute logic, copied verbatim with no cross-boundary import per `.claude/rules/research-artifacts.md`), `tools/vdata-generator/replay.js` (exports `buildVData({obj, order, overrides})` — merges overrides via spread, calls `buildPlaintext`, pipes through the Phase 43 `encodeVData`), a `replay` CLI subcommand in `tools/vdata-generator/cli.js` with `--obj`, `--order`, `--overrides`, `--self-check`, `--verbose` flags (existing cipher-only mode with `--plaintext-hex` preserved unchanged), and a README update documenting both entry points (Phase 43 cipher-only + Phase 44.5a replay) plus CLI usage examples and the future 44.5b from-scratch plan. Self-check fixtures encoded as `SELF_CHECK_CASES` inside `cli.js` sourced from `research/vm-slide-stack-vm/FINGERPRINT-SCHEMA.md`.

**Verification**: Director ran `node tools/vdata-generator/cli.js replay --self-check` → both committed fixtures byte-identical (plaintext + vData). `git diff --stat` against protected paths (research/vm-slide-stack-vm/build-fingerprint-plaintext.js, tests/fixtures/, docs/, targets/, sample/) returned empty — all untouched. `npm test` → 411/411 pass, 0 fail (prior history entry from 44.4 said 193/221; the suite grew through Phase 43/44 and 411 is the current stable baseline). Cipher-only mode sanity-tested: `node tools/vdata-generator/cli.js --plaintext-hex <jsdom-plaintext>` emitted the expected 152-char vData string ending in `YY` padding. All verification items passed.

**Notes**: Untracked `output/vdata-generator-smoke/` directory contains the JSON files the agent used for ad-hoc CLI verification — not load-bearing, kept in the working tree for now, may be cleaned up later. No modifications to protected paths. No test files written (impl/tests separation respected — 44.6 owns tests). Baseline for npm test updated: the `193/221` figure in the 44.4 history entry was stale — current stable baseline is 411/411.

---

## 44.5b: From-scratch `build-vdata.js --from-obj` full synthesis builder

**Outcome**: passed — exceeded spec.

**What was done**: Subagent shipped `tools/vdata-generator/build-from-obj.js` (exports `buildVDataFromObj({obj, seed, order})`, `SCHEMA`, `mulberry32`, `shuffleSchema`), added a `from-obj` CLI subcommand to `tools/vdata-generator/cli.js` with `--obj`, `--seed`, `--order`, `--self-check`, `--verbose` flags, and updated the README with a `from-obj` section documenting all three modes (default nondeterministic `Math.random`, `--seed` deterministic via mulberry32, `--order` explicit override). mulberry32 inlined as ~8 lines — no new npm deps. Created `output/vdata-generator-smoke/seed-sweep.js` as the scratch harness for the seeded-PRNG experiment.

**Seeded-PRNG experiment — SUCCESS (beat 30-min budget in <1 second)**: Swept seeds 0..200000 under Node 20 TimSort with the exact fn 23898 comparator `() => rng() > 0.5 ? -1 : 1`. Found working seeds for BOTH committed fixtures (spec only required one):

| Fixture | Captured order | Seed (Node 20 TimSort) |
|---|---|---|
| HAR | `[inf,env,tp,cLod,version,key,ss,py]` | `53818` |
| jsdom | `[inf,env,tp,key,py,ss,cLod,version]` | `84121` |

Seeds are stable for a given Node major version's sort algorithm but NOT portable across engines. This caveat is documented in `build-from-obj.js` header comment and in the README.

**Verification**: Director ran `node tools/vdata-generator/cli.js from-obj --self-check` → `[har] path=order vdata: MATCH`, `[jsdom] path=seed(seed=84121) vdata: MATCH`, final `from-obj self-check: OK (both fixtures byte-identical; order + seed paths)`. `replay --self-check` still green (44.5a path unbroken). Cipher-only mode (`--plaintext-hex`) still works. `npm test` → 411/411 pass, 0 fail. `git diff --stat` against protected paths (research/, tests/fixtures/, docs/, targets/, sample/) returned empty — all untouched. All verification items passed.

**Notes**: The seed sweep succeeded so fast (<1s for 200k seeds) that the 30-minute budget was never in play. The experiment validates that fn 22317's Math.random shuffle really is a textbook Fisher-Yates anti-pattern whose output is fully predictable given a seeded PRNG under a fixed sort algorithm. For fixture round-trips the `--order` explicit path is still recommended as the portable escape hatch (works across Node versions), with `--seed` as the Node-20-specific bonus path. Auto-advancing to 44.6 per the user-approved dispatch order.

---

## 44.6: Black-box tests for vdata-generator builders

**Outcome**: passed

**What was done**: Subagent shipped `tests/test-vdata-builder.js` (224 lines) with 14 black-box tests across 5 suites: (A) `buildVData` replay round-trip — both committed fixtures reproduce `vdata_string` / `har_vdata_string` byte-identically via `{obj, order}`, empty-overrides no-op, non-empty-override (length-matched `key: 'ZZZZ'`) produces a different valid 152-char vData; (B) `buildVDataFromObj` order-override path — both fixtures round-trip byte-identically; (B') default `Math.random` path shape — 5 calls per fixture all return valid 152-char vData; (C) seeded-PRNG path — HAR seed=53818 and jsdom seed=84121 reproduce fixtures exactly (Node-20-guarded via `process.versions.node.startsWith('20.')`); (E) fixture integrity canary — asserts both JSON fixture files on disk still carry the expected top-level fields + Phase 43 alphabet + padding index 64. Shape helper `assertValidVData` asserts `length === 152`, `endsWith('YY')`, every char in the Phase 43 alphabet. File imports ONLY `buildVData` from `tools/vdata-generator/replay.js` and `buildVDataFromObj` from `tools/vdata-generator/build-from-obj.js` — no reaching into build-plaintext.js, encode.js, xtea.js, or custom-base64.js. Added `tests/test-vdata-builder.js` to `package.json`'s explicit test-script file list.

**Verification**: `npm test` → 425/425 pass, 0 fail, 0 skipped (411 baseline + 14 new). Spot-read all 224 lines of the test file to confirm: (1) imports are restricted to the two documented public-API entry points, (2) each assertion is runnable and compares against a concrete expected value (no "runs without error" laziness), (3) override test uses length-matched substitution with inline comment explaining the 112-byte plaintext budget, (4) seeded-PRNG tests correctly skip on non-Node-20, (5) fixture JSDOM_OBJ / HAR_OBJ copied verbatim from `tools/vdata-generator/README.md` Replay-mode worked examples. `git diff --stat` confirms minimal `package.json` change (one line added to the test file list). No modifications to protected paths (`targets/`, `sample/`, `tests/fixtures/`, `tools/vdata-generator/` sources, `research/`, `docs/`). Impl/tests separation respected — this was a different agent from 44.5a/44.5b.

**Notes**: Test count jumped 411 → 425 (+14). Seeded-PRNG suite's Node-20 gate means on other Node majors those 2 tests skip gracefully; the 12 other tests run everywhere. Stream B tests deliverable is complete. Next task 44.7 is director-owned docs closeout — awaiting user scope approval before execution.

---

## 44.7: Phase 44 docs closeout (director-owned)

**Outcome**: passed

**What was done**: Director-owned docs closeout — no subagent dispatched. Edits:

1. **`docs/VDATA_FORMAT.md`** — rewrote the scope note and §1 Overview to cover both halves end-to-end (added a full Chrome entry chain from fn 19604 → init → proxyXHR → fn 20140 → fn 20539 → fn 22317 → fn 13860). Expanded the ASCII pipeline diagram to show the pad + ShiftRows stages explicitly. Updated §3 with a new "Key reconciliation across builds" subsection documenting the `2e43...` (fixture-canonical) vs `34e2...` (sample/vm_slide.js runtime) distinction and flagging the fixture-build reconfirmation as an optional Phase 45 follow-up. Replaced §5 with a three-mode public-API overview (cipher-only, replay, from-obj) and added `build-plaintext.js` / `replay.js` / `build-from-obj.js` + CLI subcommand rows, programmatic examples for all three modes, and the Node-20 seed-portability caveat. Added Phase 44 provenance rows to §6. Rewrote §7 from "Open questions" to "Plaintext Schema — 8-field tdc runtime-state probe" with §7.1 field table, §7.2 Fisher-Yates shuffle walk (fn 23898, pcs 23945/23949), §7.3 pre-cipher transform chain (fn 13989 / fn 14153 / module 41), §7.4 reproducibility contract.

2. **`docs/CAPTCHA_ORCHESTRATOR.md` §6 vData subsection** — kept the Phase 42 XHR-monkey-patch narrative intact (it was already correct). Replaced the Phase 44.3 "caller-supplied body canonical reduction" paragraph with a Phase 44 closeout block that (a) names the 8-field tdc-runtime-state schema, (b) walks the full Chrome entry chain through fn 22317, (c) explicitly corrects the earlier misattribution that said the plaintext is "a reduction of arguments[0]" — it is not, it is a fresh runtime-state probe with `key` being the only field that actually reads the POST body, and (d) notes that `getCaptchaData` is bound inside vm-slide itself (not in `t_captcha_slide.js`, confirmed by 44.4.5's zero-reference grep).

3. **`CLAUDE.md`** — updated the "vm-slide stack VM" header to reflect Phase 43+44 solved. Added a new "vData plaintext-build — Phase 44 solved (2026-04-15)" Project Memory block covering: 8-field schema + fn 22317 identity, fn 23898 Math.random shuffle, fn 13860 pad+ShiftRows+XTEA transform chain, live Chrome entry chain via fn 19604 → proxyXHR → fn 20140 → fn 20539 → fn 22317, and the three-mode `tools/vdata-generator/` public API with 425/425 test count. Added an "XTEA key reconciliation (Phase 44.0.1 → 44.2.7)" block documenting the two-build key story and flagging Phase 45 as an optional follow-up. Updated the "Known limitations" list to reflect Phase 44 closed. Updated the `docs/VDATA_FORMAT.md` row in the Documentation table to reflect end-to-end scope.

4. **`plan.md`** — marked 44.7 done in the phase table with a full outcome summary, flipped the Status block to "Phase 44 CLOSED, no active phase", and replaced the stale Current Task block with a placeholder.

**Verification**: `npm test` → 425/425 pass, 0 fail, 0 skipped, 0 cancelled, 0 todo (unchanged from 44.6 baseline). No code changes, no test changes, no touches to protected paths (`targets/`, `sample/`, `tests/fixtures/`, research notes, existing docs outside the three listed above). Doc edits spot-read for accuracy: fn 22317 / fn 13860 / fn 19604 / fn 20140 / fn 20539 / pc 20808 / pc 20473 / pc 20749 / pc 23945 / pc 23949 / pcs 13931/15149 / alphabet `"0abcdefghijklmnop"` / PERM `[0,4,8,12,5,9,13,1,10,14,2,6,15,3,7,11]` / seeds 53818/84121 / 425 test count — every value cross-checked against its source history entry (44.0.1, 44.2.7, 44.2.8, 44.4, 44.4.1, 44.5a, 44.5b, 44.6) or the running test suite.

**Notes**:
- The `tp` helper decomposition was hard to fit cleanly into a single table cell — kept the field table terse and deferred the prose-level helper notes to `research/vm-slide-stack-vm/FINGERPRINT-SCHEMA.md`, which §7.1 now points to.
- Left the `research/vm-slide-stack-vm/VDATA-PIPELINE.md` §8 "open questions" file unchanged — it is a historical research note, not a public doc, and `docs/VDATA_FORMAT.md` §7 now supersedes it as the authoritative reference. VDATA-PIPELINE.md can be retired in a future cleanup if desired.
- Left `research/vm-slide-stack-vm/VDATA-TRACE.md` and `VDATA-RESOLUTION.md` alone for the same reason — they are Phase 42 research notes; `CAPTCHA_ORCHESTRATOR.md` §6 is the public artifact.
- Untracked `output/vdata-generator-smoke/` scratch dir from 44.5a/44.5b is still in the working tree. It is not load-bearing and not committed; can be `rm -rf`'d at any time.
- **Phase 44 is now closed end-to-end.** vm-slide vData is byte-identically reproducible from scratch via `tools/vdata-generator/build-from-obj.js`, with the full pipeline documented in `docs/VDATA_FORMAT.md` and the Chrome entry chain documented in `docs/CAPTCHA_ORCHESTRATOR.md` §6. Stream A (44.1–44.4, discovery) + Stream B (44.5a–44.7, deliverables) are both complete. Optional Phase 45 follow-up: reconfirm which live vm-slide build generated the committed fixtures (to resolve the `2e43...` vs `34e2...` key discrepancy).

## 45.1: Per-field source decisions for live scraper vData use

**Outcome**: passed (with plan-revision finding for 45.2)

**What was done**: Classified all 8 vm-slide vData plaintext fields (`tp`, `key`, `py`, `env`, `version`, `cLod`, `inf`, `ss`) as port-as-code / profile-supplied / inline-default. Pinned browser-like default values from the HAR fixture. Specified the `computeKeyField(body)` port target for 45.2 with input/output/dependencies. Drafted the `profiles/vdata-browser-default.json` template 45.4 will create. Deliverable: `research/vm-slide-stack-vm/PHASE-45-FIELD-SOURCES.md` (272 lines, 4 sections: Decision Table, Key Digest Porting Spec, Browser Profile Template, Open Questions/Rejected Alternatives). Final classifications — `key`: port-as-code; `tp`, `ss`, `py`: profile-supplied; `env`, `version`, `cLod`, `inf`: inline default.

**Verification**: All four required sections present with correct headings. Decision Table covers 8/8 fields with classification + justification + browser-like default. Independent CLI sanity check from the director: `node -e "const {buildVDataFromObj}=require('./tools/vdata-generator/build-from-obj.js'); const obj={tp:'7446039806946242560',key:'AAAA',py:'0',env:'0',version:'2',cLod:'loadTDC',inf:'iframe',ss:'11%2Ctdc%2Cslide%2Cvm'}; const r=buildVDataFromObj({obj, order:['inf','env','tp','cLod','version','key','ss','py']}); console.log('len=',r.length,'tail=',r.slice(-2));"` → `len= 152 tail= YY`. No modifications to `targets/`, `sample/`, `tests/fixtures/`, `tools/`, or any existing research note. Exactly one new file created.

**Notes**:
- **Plan-revision finding**: the 45.1 brief assumed the `key` digest was already implemented in `research/vm-slide-stack-vm/build-fingerprint-plaintext.js`. It is NOT — the reference impl takes `obj.key` as a caller-supplied captured string and never recomputes it. Per §2.1 of the new doc, the accumulator loop at fn 22317 pcs 23240..23328 depends on `require(18)(body, "tlg")` (digit-index string) and `require(18)(body, "sess")` (charset). **Module 18 has not been decompiled** — it is the blocker for 45.2. Director adding a new prerequisite task 45.1a (module 18 decompile) before 45.2; Phase 45 re-presented to user for review before further dispatch.
- The brief's example `require('./tools/vdata-generator')` doesn't resolve — `tools/vdata-generator/` has no `index.js`. Correct entry path is `./tools/vdata-generator/build-from-obj.js`, which matches the tool's README. Noted in §3.1 of the deliverable so 45.2/45.4 don't trip on it.
- The HAR fixture's field order (`inf, env, tp, cLod, version, key, ss, py`) was used for the deterministic CLI sanity check; the Fisher-Yates shuffle in fn 23898 means the production path is nondeterministic and both orderings produce valid 152-char outputs.

## 45.1a: Decompile module 18 (`tlg` / `sess` body-parser lookups)

**Outcome**: passed

**What was done**: Decompiled vm-slide's webpack module 18 — the two-argument helper `require(18)(body, tag)` that fn 22317's `obj.key` accumulator calls twice (once with `"tlg"` at pc 22820, once with `"sess"` at pc 23022). Disassembly confirms both call sites in `output/vm-slide/disassembly-full.txt`. Module 18 is a top-level form-urlencoded field extractor: returns the raw substring between `&<tag>=` (or the leading `<tag>=`) and the next `&` or end-of-string, with NO URL-decoding, NO JSON parsing, NO value normalisation, and empty-string on missing tag. Delivered two files: `research/vm-slide-stack-vm/module18-body-parser.js` (exports `lookup(body, tag)` + `computeKeyField(body)`, self-test harness under `require.main === module`) and `research/vm-slide-stack-vm/MODULE-18-BODY-PARSER.md` (§1 Location, §2 Decompiled Pseudocode, §3 Tag Behaviour Table, §4 Edge Cases, §5 HAR Reproduction Proof).

**Verification**: Independent director run of `node research/vm-slide-stack-vm/module18-body-parser.js` from the repo root printed the full HAR oracle trace — verify POST body length 9504 (9345 pre-`&vData=` injection), first 80 bytes `aid=2046626881&protocol=https&accver=1&showtype=popup&ua=TW96aWxsYS81LjAgKFdpbmR`, `lookup(body, "tlg") = "8128"`, `lookup(body, "sess") length = 322` starting with `"s1LCqg-Z2OZiIDOktcwDJ4mtzyDd91soncHQX79s"`, digit array `[8,1,2,8]`, charAt walk `sess[8]="2", sess[1]="1", sess[2]="L", sess[8]="2"` → computed `obj.key = "21L2"` matching the HAR oracle — `PASS: obj.key = "21L2" reproduced from HAR verify POST body.` with exit code 0. Both files are new; no modifications to `targets/`, `sample/`, `tests/fixtures/`, `tools/`, `docs/`, or any existing research note.

**Notes**:
- **Incidental finding worth surfacing for 45.2/45.4**: `sample/t_captcha_slide.js` sets `e.tlg = e[collectdata].length` on the orchestrator object before serialising the verify body — i.e. `tlg` is the integer length of the `collect` token as a string, which is why every `tlg` character is a base-10 digit and the `parseInt`/`charAt` chain never OOBs. The scraper's `buildVDataForPost` wiring in 45.2+45.4 must ensure the serialised POST body contains a valid `tlg=<N>` substring (where N is the collect-token length) before calling `computeKeyField`. This is a caller precondition documented in the 45.2 Current Task block.
- Module 18 lives in vm-slide's OWN webpack graph, not the orchestrator bundle's — `require(18)` inside fn 22317 is a slot 18 lookup in vm-slide's per-module dispatch table, distinct from the webpack graph in `sample/t_captcha_slide.js`.
- The `[4, 2, 3, 10]` fallback branch in fn 22730 is the IE9-below path; walked over the default `"abcdefghijklmn"` charset it yields `"ecdk"`. The scraper will never hit this branch (Chrome predicate path is always truthy for our fingerprint), but `computeKeyField` in 45.2 will optionally support it behind an `ie9Fallback` flag.
- The URL-decoding invariant matters: module 18 does NOT decode, so the digit-indexing is over raw bytes of the `sess` value. Any future scraper body that percent-encodes `sess` or `tlg` would be walked character-by-character over the encoded bytes. Preserved as an invariant in the 45.2 port.

## 45.2: Port `computeKeyField` + add `buildVDataForPost` entry point

**Outcome**: passed

**What was done**: Productised the 45.1a reference impl into `tools/vdata-generator/`. Four changes landed: (1) new `tools/vdata-generator/build-key-field.js` exporting `lookupFormField(body, tag)` + `computeKeyField(body, {ie9Fallback})` — ported verbatim from `research/vm-slide-stack-vm/module18-body-parser.js` with the added `ie9Fallback` option for the fn 22730 false-branch `[4, 2, 3, 10]` fallback; (2) new `tools/vdata-generator/for-post.js` exporting `buildVDataForPost(postBody, {profile, overrides, order, ie9Fallback})` — validates profile shape, enforces `'__COMPUTED__'` sentinel on `profile.key`, rejects `overrides.key`, merges then calls `buildVDataFromObj`; (3) `tools/vdata-generator/cli.js` gained a `for-post` subcommand mirroring `from-obj` with flags `--body`, `--profile` (both `@file`-capable), `--overrides`, `--order`, `--ie9-fallback`; (4) `tools/vdata-generator/README.md` gained a fourth programmatic-API mode entry + "Caller preconditions" subsection documenting the `tlg=<digits>` / `sess=<≥14 chars>` invariants and the `profile.key` sentinel rule. No test files (45.3's job).

**Verification**: Director ran two independent spot-checks from the repo root. (1) HAR oracle via programmatic entry: `computeKeyField` on the real HAR verify POST body (with `&vData=...` tail stripped) returned `"21L2"` exactly, matching the `tests/fixtures/vdata-har-capture.json` recorded key. (2) CLI wiring: `node tools/vdata-generator/cli.js for-post --body 'tlg=8128&sess=s1LCqg-Z2OZiIDOktcwDJ4mtzyDd91soncHQX79s' --profile '<§3 browser profile>'` printed a 152-char string with `len=152 tail=YY`. Regression guard: `node --test tests/test-vdata-builder.js tests/test-vdata-generator-encoder.js` reports 72/72 pass, 0 fail, 0 skipped, 0 todo. No modifications outside `tools/vdata-generator/`.

**Notes**:
- URL-decoding invariant preserved: both `lookupFormField` and `computeKeyField` walk raw body bytes, no `decodeURIComponent`. This is load-bearing for future scraper bodies that might percent-encode `sess` or `tlg`.
- The `ie9Fallback` option honours fn 22730's false branch (`require(0)() === false` path). The scraper will never hit this — the Chrome predicate is always truthy for our fingerprint — but the option is there so `computeKeyField` matches vm-slide's complete behaviour surface. Tested in 45.3 group D.
- Nondeterministic CLI caveat: without `--order`, the `from-obj` shuffle produces a different 152-char string on every invocation. That's correct behaviour (matches `buildVDataFromObj`'s default path) but means the CLI self-check is a shape check, not a byte-identity check. Byte-identity against the HAR fixture requires passing `order` explicitly (45.3 group F).

## 45.3: Tests for `computeKeyField` + `buildVDataForPost`

**Outcome**: passed

**What was done**: Shipped `tests/test-vdata-for-post.js` — 37 black-box scenarios across 7 groups locking down the 45.2 pipeline. Group A (9 scenarios) exercises `lookupFormField` edge cases: prefix-substring collision (`'foobar=x&foo=y'` → `'y'`, not `'x'`), value-containing-`=` (`'a=b=c'` → `'b=c'`), tag-at-tail without trailing `&`, and URL-encoded value preservation (`'tag=a%26b'` → `'a%26b'`, locks in the no-decode invariant). Group B is the load-bearing HAR oracle — loads `sample/captcha-har.har`, strips the injected `&vData=...` tail, and asserts `computeKeyField(body) === '21L2'`. Group C runs the same assertion against a synthetic minimum body. Group D covers `computeKeyField` edge cases including `ie9Fallback: true` → `'ecdk'` (digits `[4,2,3,10]` walked over the `'abcdefghijklmn'` default sess). Group E asserts `buildVDataForPost` shape — 152 chars, every char in the Phase 43 alphabet, `YY` tail, stable across the nondeterministic shuffle. Group F is the end-to-end byte-identity proof: passes the HAR body + HAR fixture's profile (with key stripped) + HAR fixture's order through `buildVDataForPost`, asserts the result equals the fixture's recorded vData byte-for-byte. Group G (11 sub-scenarios) locks down input validation: `profile.key === '__COMPUTED__'` is the only non-undefined sentinel accepted, `overrides.key` is always forbidden, non-string and empty-string bodies throw, falsy profile keys throw.

**Verification**: Director ran `node --test tests/test-vdata-for-post.js` → `# tests 37 # pass 37 # fail 0`, and `npm test` → `# tests 462 # suites 110 # pass 462 # fail 0 # duration_ms 31321`. Groups B and F both green — the HAR byte-identity gate holds end-to-end. No modifications outside `tests/test-vdata-for-post.js` except a one-line `package.json` edit (see Notes).

**Notes**:
- **Deviation from spec**: agent added `tests/test-vdata-for-post.js` to the `npm test` explicit file list in `package.json`. The project runs `npm test` via an enumerated list (not a glob), so without this addition the new tests would not be picked up by the full suite and the verification item "`npm test` exits 0 with count increased from 425" would be unobservable. The diff is a single-token insertion on the test script line — accepted.
- Group G was implemented as 11 sub-tests (G1a–G1e for the five bad body types, plus G2, G3, G4, G5, G6a, G6b). Per-type `assert.throws` granularity makes diagnosis easier when a future refactor breaks one specific path.
- Group F passes without any plaintext-level debugging — it worked on the first run, which is strong evidence that `buildVDataForPost` + `buildVDataFromObj` + the Phase 43 cipher chain compose cleanly and that no field-order surprise creeps in when the explicit `order` is provided.
- 45.2 + 45.3 close out Stream A of Phase 45. Stream B starts with 45.4 (scraper wiring swap).
