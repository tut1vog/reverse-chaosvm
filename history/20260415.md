# 2026-04-15

## 44.4: Per-field value-source pin + pre-cipher transform check

**Outcome**: passed

**What was done**: Subagent ran the split-on-`&/=` pre-check (outcome 2 — garbled keys), then decompiled webpack module 40's `encryptData` (fn 13860) into a 3-stage chain: PKCS#7-style padder (fn 13989, alphabet `"0abcdefghijklmnop"`, `padLen = 16 - (len % 16)`), AES-ShiftRows-style 16-byte permutation (fn 14153, `PERM = [0,4,8,12,5,9,13,1,10,14,2,6,15,3,7,11]`), and Phase 43's XTEA (module 41). Inverted the permutation to recover clean `key=value` pairs from both fixtures. Decompiled 3 of fn 22317's 4 nested helpers: fn 22400 builds `tp` (tp-probe), fn 22730 builds the digit-array consumed by `key` (via `require(18)(body,"tlg")` → char-lookup loop), fn 23399 builds `ss`. Other 5 fields inline in fn 22317: `version` = `"2"` literal, `py` = `arguments[1].py`, `env` = `require(0)() ? "0" : "1"`, `inf` = `window===window.top ? "top":"iframe"`, `cLod` = TDC state probe (`loadTDC` / `unloadTDC`). Delivered `research/vm-slide-stack-vm/FINGERPRINT-SCHEMA.md` (6 sections) and `research/vm-slide-stack-vm/build-fingerprint-plaintext.js` (reference impl, not productized).

**Verification**: Independently re-ran `encodeVData(buildFingerprintPlaintext({obj, order}))` against both committed fixtures. All four assertions passed byte-identically: jsdom plaintext match, jsdom vdata match, HAR plaintext match, HAR vdata match. FINGERPRINT-SCHEMA.md present with all six required sections. build-fingerprint-plaintext.js exports `buildFingerprintPlaintext` as required. No modifications to protected paths (`targets/`, `sample/`, `tools/vdata-generator/`, `tests/fixtures/`, `docs/`, existing FN-*-DECOMPILE.md). npm test: 193/221 — unchanged from baseline (confirmed via stashed-check). The plan's `411/411` target was stale; actual baseline is 193/221 pre-existing, no regressions introduced.

**Notes**:
- Two new facts surfaced that may warrant plan restructuring before 44.5b:
  1. **Sort-order contradiction**: 44.2.6 reported fn 22317's `.sort()` at pc 23949 produces alphabetical order `[cLod,env,inf,key,py,ss,tp,version]`, but both committed fixtures ship non-alphabetical orders (jsdom `[inf,env,tp,key,py,ss,cLod,version]`, HAR `[inf,env,tp,cLod,version,key,ss,py]`). Either 44.2.6's reading of the sort call was wrong, or the comparator (fn 23898) is non-lexicographic — needs a re-look. Reference impl currently takes `order` as a caller input rather than computing it.
  2. **XTEA key drift**: `sample/vm_slide.js` bakes `34e2c8f07b5169ad` at pc 13931, but both committed fixtures encrypt with `2e430f8c15b7da96` (the Phase 43 constant). The committed fixtures are from a different vm-slide build than `sample/vm_slide.js`.
- Director stopping for user review before dispatching next task — these two findings may affect 44.4.5 / 44.5b scope.
- `cLod` state-machine region (pcs 23059..23224) is not exhaustively walked; fallback values `loadTDC`/`unloadTDC` observed in fixtures but full branching not documented.
- `require(18)` (module 18's role for the `key` field) not fully decompiled.

## 44.0.1: Bytecode build reconciliation — XTEA key drift

**Outcome**: passed

**What was done**: Subagent grepped `output/vm-slide/bytecode.json` (via per-char `OP_10`-run scan, not raw ASCII) for both key strings. Result: `34e2c8f07b5169ad` found at pcs 13931 and 15149 as 16 `OP_10` pushes inside `OP_04 ... OP_02 2 OP_49` array builders, then passed as an argument via `OP_58 13860 ...` at pc 13970; `2e430f8c15b7da96` NOT present anywhere in the bytecode (nor in `sample/vm_slide.js` source text). Subagent cross-checked `research/vm-slide-stack-vm/vdata-dynamic-trace.js` and found Phase 43 captured the key by sampling **local slot 4** of the encrypt closure's stack frame at the first dispatch tick of fn 13860 — i.e. after the function prologue's pre-cipher transform had run. Confirmed `tools/scraper/vdata-harness.js` and `research/vm-slide-stack-vm/vdata-dynamic-trace.js:48` both load `sample/vm_slide.js` by default, and the HAR fixture's `xtea_key_hex` matches the jsdom fixture's — so `sample/vm_slide.js` is unambiguously the fixture-generating build. Delivered `research/vm-slide-stack-vm/BUILD-RECONCILE.md` (~2 pages).

**Verification**: Director spot-checked disassembly pcs 13921..14008 via sed — confirmed `OP_04` at 13930, 16 `OP_10 <charcode>` pushes at 13931..13961 exactly matching the ASCII bytes of `34e2c8f07b5169ad`, `OP_02 2 OP_49` at 13963..13965, `OP_58 13860 3 1 7 10 8 11 10 9 3` at 13970. Matches the subagent's report verbatim. BUILD-RECONCILE.md present with verdict (A)+nuance clearly stated, evidence organized into 4 labeled sections, implications/nomenclature/cross-checks documented. No modifications to protected paths.

**Notes**:
- Verdict: **(A) with nuance** — `sample/vm_slide.js` IS the fixture-generating build; BOTH keys are real and from the same build. `34e2c8f07b5169ad` is the **bytecode-literal pre-cipher seed** (pushed at pcs 13931 and 15149 into fn 13860); `2e430f8c15b7da96` is the **runtime XTEA round key** observed in the encrypt closure's local 4 after fn 13860's prologue has transformed the seed. 44.4's "Finding 2" is reclassified, not dismissed.
- No re-decode of the fixture build needed; all Phase 44 pc anchors (fn 22317, 13860, 13989, 14153, 22400, 22730, 23399) stand valid.
- Recommended nomenclature for Phase 44 downstream work: `xtea_seed_hex` for the bytecode literal vs `xtea_key_hex` for the runtime round key.
- Pinning the seed→key transform itself is a new Phase 44 follow-up — not required for 44.4.1 or 44.5b, since the reference encoder already uses the runtime key.
- CLAUDE.md Project Memory §Phase 42 corrected: previous wording claimed `2e430f8c15b7da96` was the "bytecode constant" — that was imprecise. Now correctly distinguishes runtime key vs seed with a pointer to BUILD-RECONCILE.md.
